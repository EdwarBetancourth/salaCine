import { AdminRolDirective } from './admin-rol.directive';

describe('AdminRolDirective', () => {
  it('should create an instance', () => {
    const directive = new AdminRolDirective();
    expect(directive).toBeTruthy();
  });
});
